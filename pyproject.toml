[build-system]
requires = ["setuptools>=61.0", 'wheel']

[project]
name = "uiucprescon.build"
version = "0.1.0"
dependencies = [
    "setuptools>=61.0",
    'wheel',
    "conan>=1.53,!=1.55.0",
    "cmake",
    'pybind11~=2.10.1',
    "conan>=1.53,!=1.55.0,<2.0",
    'toml'
]
